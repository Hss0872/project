<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.best.team.community.dao.MemberDao">
    <insert id="join" parameterType="Member">
        INSERT INTO MEMBERTEST VALUES(#{m_id}, #{m_pwd}, #{m_name}, #{m_birth}, #{m_addr}, default, #{m_email}, #{authkey}, default)
    </insert>
</mapper>